# AI Document Assistant Pro

Программа для интеллектуального анализа документов на русском языке.  
Загружает файлы (PDF, изображения, TXT), извлекает текст (включая OCR), строит векторную базу знаний и отвечает на вопросы по содержимому документов с помощью LLM через OpenRouter.[file:42]

---

## 1. Возможности

- Загрузка документов: `txt`, `pdf`, `png`, `jpg`, `jpeg`.[file:42]  
- Извлечение текста:
  - из PDF через `PyPDF2` по всем страницам;
  - через OCR (`easyocr`) для сканов и «немых» PDF.[file:42]  
- Построение векторной базы знаний в SQLite (`knowledge_base_pro.db`) на базе `sentence-transformers`.[file:42]  
- Ответы на вопросы по документам с учётом контекста.  
- Опциональное подключение веб‑поиска (DuckDuckGo) при недостатке данных из документов.[file:42]  
- Сохранение ответов в DOCX в папку `Ответы ИИ-агента`.[file:42]  
- Кнопка очистки базы знаний в интерфейсе.

---

## 2. Установка (для разработчика)

### Требования

- Windows 10/11  
- Git  
- Python 3.10+  
- Аккаунт на [openrouter.ai](https://openrouter.ai) и API‑ключ

### Шаги

git clone https://github.com/Romann37/dialog_pro.git
cd dialog_pro

python -m venv venv
venv\Scripts\activate

pip install --upgrade pip
pip install -r requirements.txt

text
[web:51][file:42]

Создайте файл `.env` в корне проекта:

OPENAI_API_KEY=ваш_ключ_OpenRouter

необязательно, можно не указывать — тогда возьмётся модель из CONFIG
ID_MODEL=meta-llama/llama-3.2-3b-instruct:free

text
[file:42]

(Файл `.env` не должен попадать в git — он игнорируется через `.gitignore`.)

---

## 3. Запуск в режиме разработчика

Активируйте виртуальное окружение и запустите Streamlit:

venv\Scripts\activate
streamlit run dialog_pro.py

text
[file:42]

Обычно приложение откроется по адресу `http://localhost:8501` в браузере.

---

## 4. Как пользоваться

1. **Загрузка документов**

   В левом сайдбаре, раздел **📁 Документы**:

   - Нажмите «Загрузить документы» и выберите файлы (`txt`, `pdf`, `png`, `jpg`, `jpeg`).  
   - Дождитесь сообщений «✅ \<файл\> загружен и добавлен в базу».[file:42]

2. **Интернет‑поиск (опционально)**

   В разделе **🌐 Интернет-поиск**:

   - Флажок «Использовать интернет‑поиск» — включить/выключить DuckDuckGo.  
   - Настройки:
     - «Мин. длина контекста (символы)» — если контекста из документов меньше этого значения, добавится веб‑поиск.  
     - «Мин. число чанков» — минимальное количество текстовых фрагментов в базе, чтобы не обращаться в веб.  
     - «Результатов веб‑поиска» — сколько сниппетов подтягивать из DuckDuckGo.[file:42]

3. **Задать вопрос**

   - В главной части экрана введите текст в поле «❓ Задайте вопрос по документам».  
   - Нажмите кнопку **🚀 Спросить ИИ**.  
   - В блоке «✅ Ответ ИИ» появится ответ, составленный на основе ваших документов и (при необходимости) интернета.[file:42]

4. **Сохранение ответа в DOCX**

   - Под ответом нажмите **💾 Сохранить ответ в DOCX**.  
   - Файл с ответом будет сохранён в папку `Ответы ИИ-агента` с отметкой времени.[file:42]

5. **Очистка базы знаний**

   В сайдбаре, раздел **⚙️ Настройки**:

   - Отображается количество чанков: `Чанков в базе: N`.  
   - Нажмите **🗑️ Очистить базу знаний**, чтобы удалить все сохранённые текстовые фрагменты (контент из документов) из SQLite‑базы.[file:42]  
   - После очистки ИИ перестанет использовать старые документы.

---

## 5. Сборка exe (для пользователя без Python)

Репозиторий содержит вспомогательный скрипт `run_app.py`, который запускает Streamlit‑приложение.[web:47][file:42]

### 5.1. Зависимости для сборки

В рабочем окружении разработчика должны быть установлены:

pip install -r requirements.txt
pip install pyinstaller

text
[web:51]

Также используется хук для Streamlit:

hooks/hook-streamlit.py
from PyInstaller.utils.hooks import copy_metadata

datas = copy_metadata("streamlit")

text
[web:47]

### 5.2. Сборка exe

1. Убедитесь, что в venv нет старого backport‑пакета `pathlib` (PyInstaller с ним конфликтует):

python -m pip uninstall -y pathlib

text

2. Из корня проекта (где лежит `run_app.py` и папка `hooks`) выполните:

pyinstaller --onefile --additional-hooks-dir=./hooks run_app.py --clean

text
[web:47][web:50]

3. После завершения сборки в папке `dist` появится файл `run_app.exe`.

---

## 6. Как пользоваться exe на другом компьютере

Пользователю без Python нужно только содержимое папки `dist` плюс рабочие директории:

1. Скопировать к себе папку, например `dialog_pro_app`, в которую входят:
- `run_app.exe`
- папка `documents` (можно пустую)
- папка `Ответы ИИ-агента` (можно пустую)
- при необходимости `config.json` и `.env` (если нужна своя конфигурация и API‑ключ).
2. Дважды кликнуть `run_app.exe`.  
3. Подождать несколько секунд, пока поднимется встроенный Python и Streamlit.  
4. В браузере автоматически откроется интерфейс ассистента — дальше работа как в обычной веб‑версии.[web:47][file:42]

---

## 7. Структура репозитория

Основные файлы и директории:

- `dialog_pro.py` — основной код приложения Streamlit.[file:42]  
- `run_app.py` — запускной скрипт для билда exe.  
- `hooks/hook-streamlit.py` — хук для PyInstaller, чтобы корректно упаковать Streamlit.[web:47]  
- `requirements.txt` — список зависимостей.  
- `.gitignore` — исключения (виртуальное окружение, базы, артефакты сборки и т.п.).  
- (во время работы)  
- `knowledge_base_pro.db` — SQLite‑база с векторными чанками.  
- `documents/` — загруженные документы.  
- `Ответы ИИ-агента/` — сохранённые DOCX‑ответы.

---

## 8. Безопасность и рекомендации

- Не добавляйте файл `.env` в git; он должен быть перечислен в `.gitignore`.  
- API‑ключи OpenRouter храните только локально у пользователя.  
- Перед сборкой exe убедитесь, что приложение стабильно работает в venv.  
- При изменении зависимостей обновляйте `requirements.txt` командой `pip freeze > requirements.txt`.[web:51]