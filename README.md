# AI Document Assistant Pro

Приложение для интеллектуального анализа документов на русском языке.  
Позволяет загружать файлы (PDF, изображения, TXT), извлекать текст (включая OCR), строить базу знаний и задавать вопросы к содержимому документов через веб‑интерфейс на Streamlit.[file:42]

---

## 1. Что умеет программа

- Принимает документы форматов: `txt`, `pdf`, `png`, `jpg`, `jpeg`.[file:42]  
- Извлекает текст:
  - из PDF по всем страницам (`PyPDF2`);
  - из сканов и «немых» PDF через OCR (`easyocr` + `pdf2image`).[file:42]  
- Создаёт векторную базу знаний в SQLite (`knowledge_base_pro.db`) с помощью `sentence-transformers`.[file:42]  
- Отвечает на вопросы по документам, используя модель LLM через OpenRouter.  
- При необходимости добавляет информацию из интернета (DuckDuckGo), если данных в документах мало.[file:42]  
- Сохраняет ответы в DOCX в папку `Ответы ИИ-агента`.[file:42]  
- Позволяет полностью очистить базу знаний кнопкой в интерфейсе.

---

## 2. Установка (пользователь с Windows)

### Требования

- Windows 10/11  
- Установленный Python 3.10 или новее:  
  - Скачать можно с официального сайта: https://www.python.org/downloads/  
  - При установке обязательно отметить галочку **“Add Python to PATH”**.[web:51]

### Шаги установки

1. **Скачать проект**

   Скачайте репозиторий как ZIP или клонируйте через Git:

git clone https://github.com/Romann37/dialog_pro.git
cd dialog_pro

text

2. **Создать виртуальное окружение**

В папке проекта:

python -m venv venv

text

3. **Активировать окружение**

venv\Scripts\activate

text

4. **Установить зависимости**

pip install --upgrade pip
pip install -r requirements.txt

text
[web:51]

5. **Создать файл `.env`**

В корне проекта создайте файл `.env` со строками:

OPENAI_API_KEY=ВАШ_API_КЛЮЧ_ОТ_OPENROUTER

необязательно, можно не указывать — будет использована модель из CONFIG
ID_MODEL=meta-llama/llama-3.2-3b-instruct:free

text
[file:42]

API‑ключ можно получить на https://openrouter.ai/

---

## 3. Запуск приложения

После установки зависимостей и создания `.env`:

cd dialog_pro
venv\Scripts\activate
streamlit run dialog_pro.py

text
[file:42]

- Откроется вкладка браузера (обычно `http://localhost:8501`).  
- Если вкладка не открылась автоматически, зайдите по адресу вручную:  
  `http://localhost:8501`.[web:59]

---

## 4. Как пользоваться

1. **Загрузка документов**

   - В левом сайдбаре найдите блок **📁 Документы**.  
   - Нажмите «Загрузить документы» и выберите файлы форматов `txt`, `pdf`, `png`, `jpg`, `jpeg`.  
   - Дождитесь сообщений «✅ \<имя файла\> загружен и добавлен в базу» — это значит, что текст извлечён и сохранён в векторную базу.[file:42]

2. **Настройка интернет‑поиска (опционально)**

   Блок **🌐 Интернет-поиск**:

   - Флажок **«Использовать интернет‑поиск»** — включает/выключает догрузку информации из интернета.[file:42]  
   - Параметры:
     - **Мин. длина контекста (символы)** — если текста из документов меньше этого значения, добавляется веб‑поиск.  
     - **Мин. число чанков** — минимальное количество текстовых фрагментов в базе, при меньшем значении будет использоваться интернет.  
     - **Результатов веб‑поиска** — сколько сниппетов брать из DuckDuckGo.[file:42]

3. **Задать вопрос к документам**

   - В центральной части страницы введите вопрос в поле **«❓ Задайте вопрос по документам»**.  
   - Нажмите кнопку **🚀 Спросить ИИ**.  
   - Ниже появится блок **«✅ Ответ ИИ»** с результатом.[file:42]

4. **Сохранение ответа в DOCX**

   - Под ответом нажмите **«💾 Сохранить ответ в DOCX»**.  
   - Файл сохранится в папку `Ответы ИИ-агента` в текущем каталоге проекта.[file:42]

5. **Очистка базы знаний**

   В левом сайдбаре, блок **⚙️ Настройки**:

   - Отображается количество текстовых фрагментов: `Чанков в базе: N`.  
   - Нажмите кнопку **«🗑️ Очистить базу знаний»**, чтобы удалить все сохранённые данные из SQLite‑базы `knowledge_base_pro.db` и очистить память модели.[file:42]

---

## 5. Структура папок

Основные элементы после запуска:

- `dialog_pro.py` — основной код приложения Streamlit.[file:42]  
- `knowledge_base_pro.db` — база знаний с векторными представлениями текста.  
- `documents/` — загруженные пользователем документы.  
- `Ответы ИИ-агента/` — сохранённые DOCX‑ответы.  
- `venv/` — виртуальное окружение (не отправляется на GitHub).  
- `.env` — персональные ключи (не отправляется на GitHub).

---

## 6. Безопасность

- Не передавайте файл `.env` и свои API‑ключи другим людям и не выкладывайте их в открытый доступ.  
- В репозитории на GitHub файл `.env` добавлен в `.gitignore`, чтобы ключи не попадали в историю коммитов.[web:20]

Если при установке или запуске появятся ошибки, сначала проверьте, что:

- активировано виртуальное окружение (`venv\Scripts\activate`);  
- все зависимости установлены (`pip install -r requirements.txt`);  
- файл `.env` лежит в корне проекта и содержит корректный `OPENAI_API_KEY`.